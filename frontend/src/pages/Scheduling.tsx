import React from 'react';

const Scheduling: React.FC = () => (
  <main style={{ padding: '2rem' }}>
    <h1>Schedule a Call</h1>
    <p>Booking tool integration goes here.</p>
  </main>
);

export default Scheduling;
